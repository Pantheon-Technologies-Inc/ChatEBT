/**
 * Global system prompt applied to every assistant conversation.
 * Customize this string to define the app-wide behavior for the chatbot.
 */
const baseSystemPrompt = '';

module.exports = {
  baseSystemPrompt,
};
